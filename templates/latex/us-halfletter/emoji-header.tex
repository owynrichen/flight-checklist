% Emoji/font header for pandoc LaTeX builds
\usepackage{fontspec}
% Prefer a widely-available sans font on Linux; fall back will be handled by fontspec
\setmainfont{DejaVu Sans}

% Emoji placeholders are suppressed in the PDF build so the document stays
% printable even when no emoji-capable font is installed.
\newcommand{\emoji}[1]{}

% Kneeboard optimizations: smaller global font and tighter columns for half-letter kneeboard cards
\AtBeginDocument{\fontsize{9}{11}\selectfont}
\setlength{\columnsep}{6pt}
% Smaller checkbox macro for tighter spacing (provide if not defined elsewhere)
\providecommand{\checkbox}{\raisebox{0.0ex}{\fbox{\rule{0pt}{1.2ex}\rule{1.2ex}{0pt}}}\,}
