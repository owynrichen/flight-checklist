# Required runtime Python packages (install via `uv` or pip inside your env):
# - PyYAML: parsing / writing checklist YAML
# - playwright: required for HTML→PDF rendering (Chromium)
PyYAML
playwright
